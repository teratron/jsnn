'use strict'

/*import New from './jsnn';

const nn = New('perceptron');

nn.SetBias(true);

console.log(nn)

nn.Init('ppp5');//
nn.Train();
nn.Verify();
nn.Query();*/

/*let name = 'name.json';
let ext = name.includes('.') ? name.split('.') : '';
console.log(ext, name.substr(name.lastIndexOf('.')) );*/

//export {New};
